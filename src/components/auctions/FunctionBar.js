import React, { useRef} from "react";
import {Button} from 'react-bootstrap'

export const FunctionBar = () => {

  return (
    <div>
        <Button variant="info" id="watchlistbutton">
            WatchList
        </Button>
    </div>
  );
};
